schema: UnifiedDB
version: '0.1'
parameters:
- name: CACHE_BLOCK_SIZE
  description: Implementation-specific size of a cache block that is discoverable by software.
  schema:
    type: integer
  constraints:
    implementation_specific: true
    discoverable_by_software: true
    system_wide_uniformity_in_the_initial_set_of_cmo_extensions: true
  definedBy: Snippet1
  isaVisible: true
  confidence: high
  evidence:
    trigger: implementation-specific
    excerpt: The capacity and organization of a cache and the size of a cache block are both implementation-specific, and
      the execution environment provides software a means to discover information about the caches and cache blocks in a system.
      In the initial set of CMO extensions, the size of a cache block shall be uniform throughout the system.
rejected:
- candidate: CACHE_CAPACITY
  category: Microarchitectural Detail
  reason: NOT_ISA_VISIBLE
  excerpt: The capacity and organization of a cache and the size of a cache block are both implementation-specific
  explanation: The passage identifies cache capacity as implementation-specific but does not explicitly state that architectural
    behavior depends on it.
- candidate: CACHE_ORGANIZATION
  category: Microarchitectural Detail
  reason: NOT_ISA_VISIBLE
  excerpt: The capacity and organization of a cache and the size of a cache block are both implementation-specific
  explanation: Cache organization is implementation-specific, but the supplied text does not explicitly establish ISA-visible
    behavior.
- candidate: CACHE_BLOCK_UNIFORMITY
  category: Architectural Constraint
  reason: CONSTRAINT_NOT_PARAMETER
  excerpt: In the initial set of CMO extensions, the size of a cache block shall be uniform throughout the system.
  explanation: This is a mandatory architectural requirement rather than an implementation-selected parameter.
